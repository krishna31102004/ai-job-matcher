# Python
**/__pycache__/
**/*.py[cod]
**/.pytest_cache/
**/.mypy_cache/
backend/.venv/

# Node / Frontend
frontend/node_modules
frontend/.vite
frontend/dist

# OS/editor cruft
.DS_Store
**/.DS_Store
.vscode/
.idea/

# Env & secrets (Compose passes .env at runtime; don't bake it into the image)
.env
.env.*
!frontend/.env.example
!backend/.env.example

# Git (don’t send the entire history to the build context)
.git
.gitignore

# Logs & coverage
**/*.log
coverage/
